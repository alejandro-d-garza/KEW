{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the Kew status window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
